# Техническое задание на разработку резюме-билдера

## Технологии - 3

- Чистый css, без препроцессоров (кроме autoprefixer)
- Для автоматизации задач по сборке и прочему - grunt
- Для подключения модулей в js - require.js (+ r.js для сборки в один файл)
- Для js используем:
  - jQuery
  - Backbone
  - Шаблоны underscore
- Не используем сторонних библиотек для UI
- Style Guides:
  - CSS: https://github.com/hhru/frontend/blob/master/docs/css.md
  - JS: https://github.com/hhru/frontend/blob/master/docs/js-style-guide.md
    - Использовать jshint (есть поддержка в Webstorm)
- Поддерживаемые браузеры
  - Chrome latest
  - Firefox latest
  - iPad Safari latest
- Разработку ведём в ветках в Гите и пользуемся ребейзом для получения изменений,
  а не подмердживаем мастер постоянно


## Логин пользователя

- oauth (node js) - 5
  - кнопка войти
  - по нажатию отркывается поп-ап с авторизацией
  - после авторизации переходим к первой странице

## Общие элементы на каждой странице - 0,5

- Имя текущего пользователя
- Кнопка «выход»

## Страница со списком резюме

Если у пользователя только одно резюме, показывается страница с редактированием этого резюме.

Навигация:
- Кнопка «создать резюме»

Список резюме:
- Заголовок (желаемая должность)
- Статус публикации
  - Опубликовано
  - Неопубликовано
  - На модерации
  - Требуются исправления после модерации
- Тип видимости (ссылкой на настройку видимости)
  - Видно всему интернету
  - Видно зарегистрированным на hh работодателям
  - Не видно никому
  - Видно по прямой ссылке
  - Никто, кроме белого списка
  - Все, кроме чёрного списка
- Последнее изменение
- Количество просмотров
  - Всего
  - Новых
- Кнопка обновления даты резюме
  - После нажатия показывается сообщение о том,
  что дата обновлена и время через которое можно снова обновить
  - Если перезагрузил страницу, сообщение о том, когда снова можно будет обновить
- Дублировать


## Страница редактирования

Навигация:
- Ссылка на «список резюме»
— Ссылка «создать резюме»

Нюансы:
- Если хотя-бы один блок отредактирован, но не сохранён,
а пользователь хочет уйти со страницы, уведомлять его об этом
(стандартное браузерное уведомление).
- Делаем только русскую версию резюме.
- Редактирование на блоки, у каждого блока есть кнопка «редактировать»

## Блок с текущим статусом резюме:
- Всегда на виду («плавает» при скроле)
- Прогресс заполнения
- Рекомендуемые для заполнения поля (максимум 5, остальные скрыты), порядок:
  - Обязательные поля или ошибки модерации
  - Необязательные поля
- Статус публикации резюме
  - Если заполнены все обязательные поля, дать возможность опубликовать
  - Если опубликовано и прошло модерацию показываем ссылку на настройку видимости
  - Если в модерации, пишет об этом сообщение
  - Если не прошло модерацию — выводим ошибки (в виде рекомендуемых полей)
- Тип видимости (ссылкой на настройку видимости)
  - Видно всему интернету
  - Видно зарегистрированным на hh работодателям
  - Не видно никому
  - Видно по прямой ссылке
  - Никто, кроме белого списка
  - Все, кроме чёрного списка

## Блок с дополнительной информацией
- Последнее изменение
- Количество просмотров
  - Всего
  - Новых
- Кнопка обновления даты резюме
  - После нажатия показывается сообщение о том,
  что дата обновлена и время через которое можно снова обновить
  - Если перезагрузил страницу, сообщение о том, когда снова можно будет обновить
- Дублировать
- Удалить


## Настройка видимости
- Видно всему интернету
- Видно зарегистрированным на hh работодателям
- Не видно никому
- Видно по прямой ссылке
- Никто, кроме белого списка
  - Возможность указать компании
  - Поисковая строка
- Все, кроме чёрного списка
  - Возможность указать компании
  - Сразу показывать найденной компанию, в которой работает соискатель
  - Поисковая строка


## Персональная информация
*Полный список параметров и правил валидации можно получить GET /resumes/{resume_id}/conditions*

### ФИО - 3
- **last_name** — фамилия ;
  + Поле input type="text"
  + Валидация
      * Длина в допустимом диапозоне
- **first_name** — имя;
  + Поле input type="text"
  + Валидация
      * Длина в допустимом диапозоне
- **middle_name** — отчество;
  + Поле input type="text"
  + Валидация
      * Длина в допустимом диапозоне

### Дата рождения - 0,5
**birth_date** — день рождения в апи форматируется (ГГГГ-ММ-ДД)
    + Три поля:
      + Список выбора для дня (количество дней должно зависеть от месяца и года)
      + Список выбора для месяца
      + Текстовое поле для года
    + Валидация
        * Дата в допустимом диапазоне

### Пол - 0,5
**gender** — пол. Элемент справочника gender;
    + Стилизованный radio c возможностью выбора только одного значения из справочника

### Город - 4
**area** — город проживания. Элемент справочника areas;
  + input type="text", с автодополнением
  + Сбоку иконка позволяющая вызвать полный список вариантов для поиска
    * При клике открывается дополнительная форма выбора города с полем поиска,
      и гоородами отсортированными по регионам (представленными в виде дерева,
      с возможностью свернуть и развернуть узлы, изначально свернутые)
  + Валидация
    * Значение есть в справочнике
    * У значения из справочника нет потомков

### Метро - 1,5
**metro** — ближайшая станция метро. Элемент справочника metro.
  Имеет смысл указывать только для area с метро;
  + input type="text", с автодополнением
  + Показывать в том случае если в area указан город, в котором есть метро
  + Валидация
    * Станция метро существует
    * Ближайшее метро может быть только в городе проживания

### Гражданство (citizenship) - 1
  - Две кнопки: Россия и Другие страны.
  - При выборе "Других стран" появляется multiple select со списоком стран
  - Список стран можно получить по адресу https://api.hh.ru/areas ,
    выдрав объекты с parent_id = null

### Разрешение на работу (work_ticket) - 0,5
  - Две кнопки: Россия и Другие страны.
  - При выборе "Других стран" появляется multiple select со списоком стран
  - Список стран можно получить по адресу https://api.hh.ru/areas ,
    выдрав объекты с parent_id = null
  - Почему-то при отсутсвии какой-либо выбранной страны, указывается Россия.


## Желаемая должность и зарплата

### Желаемая должность - 3
Поле ввода:
  - лейбл «Желаемая должность»
  - плейсхолдер «Например, «менеджер по продажам»
  - подсказка «Укажите полное название должности, которая вам интересна»
  - ограничение `maxlength` получается через метод api `resume_conditions`
  - валидируется заполнение поля, если поле не заполнено — поле красное,
    выводится предупреждающая иконка

### Специализация - 3,5
Селект:
  - лейбл «Профессиональная область»
  - текстовые ноды и аттрибуты `value` содержат поля `name` и `id`
    корневых элементов справочника [specializations](https://api.hh.ru/specializations)

Группа чекбоксов:
  - появляется после выбора значения в селекте
  - лейблы и аттрибуты `value` содержат поля `name` и `id`
    элементов из справочника [specializations](https://api.hh.ru/specializations),
    которые лежат внутри элемента, выбранного в селекте
  - при установке трех чекбоксов вся группа блокируется
  - подсказка «Выберите не более трех специализаций» (точное количество брать из conditions)


### Желаемая зарплата - 1
Поле ввода:
  - `type="text"` + `pattern="^[0-9]+$"`
  - валидация вводимых данных (разрешены только `[0-9]`),
    если поле заполнено неверно — поле красное, выводится предупреждающая иконка
  - ограничения на минимальное и максимальное значения получаются через
    метод api `resume_conditions`  (но на самом деле там 0 и `null`)

Переключатель с кнопками (берём из справочника api)
  - RUR
  - EUR
  - USD

### Занятость - 0,5
Переключатель с кнопками:
  - полная
  - частичная
  - проектная работа
  - волонтерство
  - стажировка

### График работы - 0,5
Переключатель с кнопками:
  - полный день
  - сменный график
  - гибкий график
  - удаленная работа

### Переезд - 3
**relocation** — возможен ли переезд в другой город. Состоит из полей:
  + type — элемент справочника relocation_type;
    * radio
    * Показывать список вариантов из справочника (relocation_type)
  + areas — город, в который возможен переезд (список). Элемент справочника areas;
    + input type="text"
      * Формируется при помощи такого-же контрола как и для поля area
      * Кнопка «добавить» для возможности указать несколько вариантов
    + сбоку иконка позволяющая вызвать полный список вариантов для поиска
      * При клике открывается дополнительная форма выбора города с полем поиска,
        и гоородами отсортированными по регионам (представленными в виде дерева,
        с возможностью свернуть и развернуть узлы, изначально свернутые)
        * Если в type выбран тип, подразумавающий возможность переезда,
          то показывать поле, иначе скрывать.
        * Можно выбрать несколько вариантов
      * Поле позволяет создать список городов, в которые доступен переезд

### Готовность к командировакам - 0,5
Переключатель с кнопками:
  - готов
  - готов иногда
  - не готов

### Время в пути (travel_time) - 0,5
  - Дропдаун
  - Список всех доступных полей можно получить из словаря
    (https://api.hh.ru/dictionaries) в "travel_time"



## Контактная информация
### Мобильный телефон:
  - поле ввода:
    - лейбл «Мобильный телефон»
    - поле ввода только одно, введенные данные разбиваются на код страны, код города и номер
  - поле ввода:
    - лейбл «Комментарий»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - чекбокс:
    - лейбл «Желаемый вид связи»
    - при установке этого чекбокса сбрасываются аналогичные чекбоксы у других контактов
  - кнопка «×» для удаления контакта

### Домашний телефон:
  - если не был указан ранее, спрятан под ссылкой «Указать домашний телефон»
  - поле ввода:
    - лейбл «Домашний телефон»
    - поле ввода только одно, введенные данные разбиваются на код страны, код города и номер
  - поле ввода:
    - лейбл «Комментарий»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - чекбокс:
    - лейбл «Желаемый вид связи»
    - при установке этого чекбокса сбрасываются аналогичные чекбоксы у других контактов
  - кнопка «×» для удаления контакта

### Рабочий телефон:
  - если не был указан ранее, спрятан под ссылкой «Указать рабочий телефон»
  - поле ввода:
    - лейбл «Рабочий телефон»
    - поле ввода только одно, введенные данные разбиваются на код страны, код города и номер
  - поле ввода:
    - лейбл «Комментарий»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - чекбокс:
    - лейбл «Желаемый вид связи»
    - при установке этого чекбокса сбрасываются аналогичные чекбоксы у других контактов
  - кнопка «×» для удаления контакта

### E-mail:
  - поле ввода:
    - лейбл «E-mail»
    - при создании резюме подставляется e-mail пользователя
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
  - чекбокс:
    - лейбл «Желаемый вид связи»
    - при создании резюме установлен по умолчанию
    - при установке этого чекбокса сбрасываются аналогичные чекбоксы у других контактов

Валидируется наличие хотя бы одного телефона. Если ни один не указан — ниже полей
выводится сообщение «Необходимо указать хотя бы один телефон» с предупреждающей иконкой.

## Ссылки на сайты
### Мой круг:
  - если не указан ранее, спрятан под ссылкой «Указать ссылку на Мой круг»
  - иконка сервиса
  - поле ввода:
    - лейбл «Мой круг»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления ссылки

### LiveJournal:
  - если не указан ранее, спрятан под ссылкой «Указать ссылку на LiveJournal»
  - иконка сервиса
  - поле ввода:
    - лейбл «LiveJournal»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления ссылки

### LinkedIn:
  - если не указан ранее, спрятан под ссылкой «Указать ссылку на LinkedIn»
  - иконка сервиса
  - поле ввода:
    - лейбл «LinkedIn»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления ссылки

### Free-lance:
  - если не указан ранее, спрятан под ссылкой «Указать ссылку на Free-lance»
  - иконка сервиса
  - поле ввода:
    - лейбл «Free-lance»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления ссылки

### Facebook:
  - если не указан ранее, спрятан под ссылкой «Указать ссылку на Facebook»
  - иконка сервиса
  - поле ввода:
    - лейбл «Facebook»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления ссылки

### Skype:
  - если не указан ранее, спрятан под ссылкой «Указать логин Skype»
  - иконка сервиса
  - поле ввода:
    - лейбл «Skype»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления контакта

### ICQ:
  - если не указан ранее, спрятан под ссылкой «Указать номер ICQ»
  - иконка сервиса
  - поле ввода:
    - лейбл «ICQ»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления контакта

### Jabber:
  - если не указан ранее, спрятан под ссылкой «Указать адрес Jabber»
  - иконка сервиса
  - поле ввода:
    - лейбл «Jabber»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления контакта

### Другой сайт:
  - если не указан ранее, спрятан под ссылкой «Указать другой сайт»
  - поле ввода:
    - лейбл «Другой сайт»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - кнопка «×» для удаления контакта

## «Начало карьеры»
  - Чекбокс
  - Если отмечен, тогда показываются специализации только для проф. области «Начало карьеры»
  - Если отмечен, опыт работы и навыки не обязательные поля

## Образование
Селект с элементами справочника `education_level`:
  - Высшее
  - Бакалавр
  - Магистр
  - Кандидат наук
  - Доктор наук
  - Неоконченное высшее
  - Среднее
  - Среднее специальное

При выборе значения «Среднее» отображается форма указания среднего образования,
при выборе остальных — форма указания высшего образования.

### Среднее образование
  - поле ввода:
    - лейбл «Название учебного заведения»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - текстовое поле:
    - лейбл «Год окончания»
    - минимальное и максимальное значения получаются через метод api `resume_conditions`
    - подсказка «Если учитесь в настоящее время — укажите год предполагаемого окончания»
  - кнопка «×» для удаления записи, если эта запись — не единственная
  - ссылка для добавления новой записи «Указать еще одно место обучения»

### Высшее образование
  - поле ввода:
    - лейбл «Название учебного заведения»
    - плейсхолдер «Например, «Московский государственный университет им. М.В. Ломоносова»
    - подсказка «Введите название или аббревиатуру учебного заведения,
      и мы предложим выбрать из списка точное название»
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - доступен саджест (не через api, а через hh.ru/autosuggest)
  - поле ввода:
    - лейбл «Факультет»
    - плейсхолдер «Например, «Механико-математический факультет»
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - доступен саджест
  - поле ввода:
    - лейбл «Специализация / специальность»
    - плейсхолдер «Например, «Прикладная математика и информатика»
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - доступен саджест
  - текстовое поле:
    - лейбл «Год окончания»
    - минимальное и максимальное значения получаются через метод api `resume_conditions`
    - подсказка «Если учитесь в настоящее время — укажите год предполагаемого окончания»
  - кнопка «×» для удаления записи, если эта запись — не единственная
  - ссылка для добавления новой записи «Указать еще одно место обучения»

### Повышение квалификации, курсы:
  - поле ввода:
    - лейбл «Название»
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
  - поле ввода:
    - лейбл «Проводившая организация»
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
  - поле ввода:
    - лейбл «Специализация / специальность»
    - плейсхолдер «Например, «Управление персоналом»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - текстовое поле:
    - лейбл «Год окончания»
    - минимальное и максимальное значения получаются через метод api `resume_conditions`
    - подсказка «Если учитесь в настоящее время — укажите год предполагаемого окончания»
  - кнопка «×» для удаления записи, если эта запись — не единственная
  - ссылка для добавления новой записи «Указать еще один курс»

### Тесты, экзамены:
  - поле ввода:
    - лейбл «Название»
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
  - поле ввода:
    - лейбл «Проводившая организация»
    - ограничение `maxlength` получается через метод api `resume_conditions`
    - валидируется заполнение поля, если поле не заполнено — поле красное,
      выводится предупреждающая иконка
  - поле ввода:
    - лейбл «Специализация / специальность»
    - плейсхолдер «Например, «Управление персоналом»
    - ограничение `maxlength` получается через метод api `resume_conditions`
  - текстовое поле:
    - лейбл «Год сдачи»
    - минимальное и максимальное значения получаются через метод api `resume_conditions`
    - подсказка «Если учитесь в настоящее время — укажите год предполагаемого окончания»
  - кнопка «×» для удаления записи, если эта запись — не единственная
  - ссылка для добавления новой записи «Указать еще один тест или экзамен»

### Владение языками (language)
  - Список языков с полем уровня
  - Каждый элемент списка имеет кнопку удаления
  - Под всеми элементами есть кнопка добавления дополнительного элемента в список
  - Элемент:
    - Дропдаун с выбором языка
      - В дропдауне отсутствуют уже добавленные языки
    - Дропдаун с уровнем языка
    - Кнопка удаления
  - Список языков можно получить по адресу https://api.hh.ru/languages
  - Список уровней языка можно получить в https://api.hh.ru/dictionaries в "language_level"

### Опыт работы (experience)
  - Список работ
  - Под всеми элементами есть кнопка добавления дополнительного элемента в список
  - Элемент:
    - Организация (company)
      - Текстовое поле
    - Регион расположения организации (area)
      - Текстовое поле с возможностью выбора обьекта из структуры https://api.hh.ru/areas
        и подсказками при вводе
    - Сайт компании (company_url)
      - Текстовое поле с проверкой корректности url
    - Отрасль (industry)
      - Дропдаун с элементами из https://api.hh.ru/industries
    - Должность (position)
      - Текстовое поле 
    - Начало работы (start)
      - Выбор месяца и года
    - По настоящее время
      - Чекбокс
    - Окончание работы (end)
      - Выбор месяца и года
      - Неактивно, если активен чекбокс "По настоящее время"
    - Обязанности и функции (description):
      - Текстовое поле
    -  Кнопка удаления

### Дополнительная информация (skills)
  - Текстовое поле для заполнения в произвольной форме

### Ключевые навыки (skill_set)
  - Список уникальных строк
  - Каждый элемент списка имеет кнопку удаления
  - Текстовое поле + кнопка добавления
  - Функциональность ввода всех навыков в одну строчку
    и автоматический парсинг по запятой
  - Есть отдельный саджест

### Список рекомендаций (recommendation)
  - Список
  - Под всеми элементами списка есть кнопка добавления дополнительного элемента в список
  - Элементы списка:
    - Имя (name)
      - Текстовое поле
    - Должность (position)
      - Текстовое поле
    - Организация (organization)
      - Текстовое поле
    - Телефон (contact)
      - Текстовое поле
      - При заполнении поля происходить автоматическое форматированние
    - Кнопка удаления 
